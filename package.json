{
  "name": "rig-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based 2D SVG rigging and animation editor that exports Jetpack Compose rig code",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
